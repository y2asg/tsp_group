/*評価値　あるアリｋが都市iにいるとき、まだ訪れていない都市jへ行くかどうかの評価値を計算する  a_(i,j,k)*/

void calucate_value(double now_per, double dist, int not_visited) { //not_visited=まだ訪れていない都市数


void caluculate_total(   ){
  double total;
int i;
  for(i=0;i< not_visited ;i++){
    siguma+=pow(now_per)
  }
